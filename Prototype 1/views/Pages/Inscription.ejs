<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Black Background Page with Image</title>
    <style>
        body {
            background-color: #000000;
            color: #ffffff;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh; /* Set the height of the body to the full viewport height */
        }

        .overlay {
            position: relative;
            width: 100%;
            max-width: 600px; /* Adjust the maximum width of the container as needed */
        }

        .overlay img {
            display: block;
            width: 100%;
            height: auto;
            position: relative;
        }

        .overlay img::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: #ef0909;
            opacity: 0.5; /* Adjust the opacity level as needed */
        }
    </style>
</head>
<body>

    <div class="overlay">
        <img src="Imgs/whiteTransp 3.png" alt="Your Image">
        <!-- Add your image source and alt text above -->
    </div>

</body>
</html>
